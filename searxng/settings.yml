# Read the documentation before extending the defaults:
# https://docs.searxng.org/admin/settings/

use_default_settings:
  engines:
    remove:
      - wikidata
      - ahmia
      - torch
      - brave
      - brave.images
      - brave.videos
      - brave.news
default_lang: "ja-JP"
server:
  secret_key: "Dj65pwvGq2Dlpvqf0LbSGC58jTc4sOo0"
  image_proxy: true
search:
  formats:
    - html
    - json
